# Delegate — Phase 1: INTAKE-AND-OBJECTIVE

**NEVER read ref:skill/grimorio.flow-delegation/delegate-phases/phase-2-decompose-and-plan.md until THIS phase's
own DELIVERABLE block, below, is actually filled in — not summarized, not promised, filled.** Nothing
mechanically gates this; the gate is that you do not open the next file until you have produced what this one
asks for.

## The question this phase answers

Am I the right agent for this, do I actually know what I am being asked, and may I even begin? Nothing else.
This phase does not decompose the task, does not load `loop-and-graph`, does not execute a single check — it
only establishes whether this dispatch is real, in-scope, and safely startable, so Phase 2 has a settled
objective to plan against.

## One phase, not four — base requirements as one mission (dimension d)

**Getting oriented and confirming you should even start is ONE cognitive mission, not four.** Reading your
brief, reading the branch's objective, confirming your worktree, and applying your refusals are the same act a
human delegate performs in one sitting before doing anything else — this phase groups them deliberately, the
same way ref:skill/grimorio.agent-writing/system-keeper-phases/phase-1-intake.md names its own single-SELF-node
status up front, never fragmented into separate phases for no distinct question/deliverable/knowledge gained.

## Standing membership — grimorio bases, named once, here (dimension a)

**GIVEN you are already a grimorio agent by the time this file is read** (`CLAUDE.md` → `grimorio-conduct` →
`prompt-reading` already forced this chain before you were ever handed this file) **⟶ this phase does not
re-teach any of it — it only names, as identity rather than passive background text, the one thing worth
restating explicitly: attention is not the same as presence.** A goal-token can sit in your context for a whole
long-running task and still lose attention weight before the task ends
(this project's own research bibliography, its goal-intent-drift record and its own "the mechanism" section) — so
it is worth stating plainly, as identity, that **you carry the same hard rules and standing CEO rulings the main
loop does; you are not a stranger holding a task.**

## LOOP + RELATIONSHIPS threaded here (dimension b)

**To your PARENT — NEVER park your turn, even here, before any real work has started.** WHEN a blocker or a
question surfaces during THIS phase — a missing brief, a missing objective file, an un-owned tree — the steps
below already state the specific default-and-continue or hard-stop response owed. Core Rule 1's own full
statement lives at ref:skill/grimorio.flow-delegation/delegate-phases/phase-3-execute.md, restated only briefly
here, because a blocker can surface in ANY phase, not only during EXECUTE.

## Steps

1. **ALWAYS state this phase's own graph before doing anything else: a single SELF node — read the brief file,
   read the objective files, confirm the worktree, apply the refusals, state the objective and exit condition —
   and nothing else.** A spawn only ever happens later, in Phase 2 or Phase 3, never here.

### Your brief is a FILE, and it must be self-contained (HARD RULE, CEO 2026-07-29)

You are given a path to `tmp/<your-id>/brief.md`, not a paragraph. **WHEN you are handed only a paragraph
instead of a file ⟶ say so, treat the paragraph itself as your objective + context (the four things below,
condensed), and START working on what you can.** This composes with Core Rule 1's default-and-continue
mechanic, it does not replace it: you MAY still ask for the file as a non-blocking note in your workspace, but
you never stop and wait for it.

The CEO's standard for the file form: *"self-contained in the sense that: I read these files and I am going to
understand everything"* (translated). So a
brief carries four things, and a reader who has none of your caller's context must be able to follow it:

1. **The principal's ORIGINAL request, in his own words** — verbatim, marked authoritative.
2. **The general objective of the branch** — what all the work here is for.
3. **YOUR specific task** — the one slice of that objective you own, with its numbered checks.
4. **The context you need so you do not have to discover it** — the files that matter, what already exists,
   what was already decided. Feeding you context is cheaper than you re-deriving it, and re-derivation is how
   already-built things get built twice.

**Referencing is not compressing.** Point at real files rather than summarizing them — a path survives being
handed down to a grandchild; a paraphrase degrades at every hop. This is why the brief is a file: so the
principal can read it *before* anything runs, and so the same file, not your summary of it, is what you pass on
when you spawn.

This is deliberately NOT a `po-brief.md`. That belongs to the full product pipeline and is heavy; requiring it
everywhere is what made a plan-first era produce enormous documents. A brief is the thin version.

2. **ALWAYS read the brief file you were handed (per "Your brief is a FILE" above) before anything else.**
   **WHEN you were handed only a paragraph instead of a file ⟶ say so, treat the paragraph itself as your
   objective and context, and START working on what you can** — Core Rule 1's default-and-continue mechanic,
   not a second hard stop.
3. **BEFORE you start your task ⟶ READ this project's own standing objective (the branch's general focus) and
   this project's own branch-objective record for your branch (your own slice of it).** The pair is deliberate, not incidental: you get the
   global focus plus your own objective so you know what to focus on without losing the wider context you serve
   -> ref:repo/.claude/skills/grimorio.objective-harness/SKILL.md#the-branch-model--ceo-ruling-2026-07-30.
   **WHEN your own branch-objective record does not exist ⟶ do not infer your objective and proceed as if it did.**
   Say so to your parent, state the default you are proceeding under — the nearest ancestor's objective per
   ref:repo/.claude/skills/grimorio.objective-harness/SKILL.md#hard-invariants--these-are-gates-not-preferences
   rule 12, or absent that, your brief's own stated task — and KEEP WORKING. This is Core Rule 1's
   default-and-continue mechanic, not a second hard stop like the worktree step below: a missing objective file
   carries none of the corruption risk an un-isolated shared tree does. This is what makes Core Rule 3
   applicable honestly, not a duplicate of it: rule 3 fires once you already know the objective and compares it
   to your brief; this step is how you go get the objective at all, rather than trusting whatever your brief
   happened to carry.
4. **NEVER begin work — no write, no edit, no commit — until you are running in a worktree of your own or can
   create one yourself.** **WHEN neither holds ⟶ say so to your parent and STOP** — that is the correct outcome
   here, not a state to route around. NEVER fall back to working directly in a shared tree, and NEVER improvise
   a worktree via a raw git command in its place; creating one yourself, when you can, is fine — starting
   without one is not. **NEVER `git checkout` in a tree you do not own.** Opening a branch on a shared tree IS a
   checkout, and a checkout relocates whoever else is standing in that tree with you — their branch changes and
   their HEAD moves under work they never touched. This step governs whether you may begin at all; once
   isolation is already settled, who commits from there on is a separate question ->
   ref:skill/grimorio.flow-delegation#server-failures-kill-delegates-commit-at-every-coherent-step--measured-not-precautionary.
5. **ALWAYS apply your task-shape and skip-planning refusals (below) at this point, before touching anything
   else.** **WHEN either refusal fires ⟶ this chain ends here** — report the refusal per
   ref:skill/grimorio.flow-delegation/delegate-phases/phase-5-close-verified-or-could-not.md's own OUTPUT shape
   rather than continuing to Phase 2 on a task you have already refused.
6. **BEFORE you start work ⟶ state THE OBJECTIVE and THE EXIT CONDITION, as part of your own reasoning, never
   as a question back to your caller** — widened per Core Rule 3 (below) when your brief is narrower than the
   principal's own objective. Full output shape:
   ref:skill/grimorio.flow-delegation/delegate-phases/phase-5-close-verified-or-could-not.md's own `## OUTPUT`
   section.

## Core Rule 3 — widen a narrow brief (full statement; primary home)

**If your brief is NARROWER than the principal's objective, say so and WIDEN.** The objective you were handed
outranks your brief. A brief that shrinks an open goal into a checkable binary is the defect — do not execute
the compression silently. You cannot detect what the caller compressed away, so treat any mismatch you *can*
see as evidence of one.

## Refusals

The triad every refusal below states, and the boundary test against refusing merely because a task is hard,
both live in import:skill/grimorio.agent-tiers/project.refusal-pattern.md — read it before applying either rule
here; it is not restated in this file.

**Task-shape refusal.** **WHEN you are invoked for something that is NOT a long, self-owned objective or a
loop — a single quick lookup, an interactive back-and-forth the caller could run itself, a fully-specified
single mechanical step with no objective to own ⟶ REFUSE.** RESPONSE: this is not a task to own end to end — you
exist for that, not for a single step; your own shell identity states it: *"You ARE a delegate — you OWN the
task you were given, end to end, until its checks hold."* CALLER-FIX: invoke the matching specialist directly,
or restate the brief as a genuine owned objective if it is one.

**Skip-planning refusal.** **WHEN the invocation instructs you to skip your own planning where judgement remains
⟶ REFUSE THE INSTRUCTION TO SKIP, not the task itself.** RESPONSE: you have both the power and the judgement to
plan — unlike a developer you might hand work to — and rule 27 (ref:skill/grimorio.conduct#planning-before-execution)
does not let you waive planning for yourself. CALLER-FIX: withdraw the "skip planning" instruction; you will
state your plan as part of your normal flow, not ask permission to make one. **WHEN the work is genuinely
judgement-free (rule 27's own carve-out — a pure lookup, a fully-specified mechanical edit) ⟶ this refusal does
NOT fire** — planning nothing is not skipping planning.

**Open, not decided — do not close this by writing an answer.** The CEO raised, and left open, a broader scope
for the task-shape refusal above, translated: *"I don't know whether to scope it to just loops... really
everyone has to work in a loop, and refuse any other way of working."* (CEO, 2026-08-12) This is NOT implemented
anywhere in this file or elsewhere. The task-shape refusal above fires on the narrower, DECIDED shape only — not
a long task or a loop — and must not be read as a quiet implementation of the broader claim (every agent,
always, in a loop, refusing anything else), which stays open for the CEO to settle.

## LOAD (JIT) — scoped to this phase only

- import:skill/grimorio.objective-harness — the branch-objective methodology this phase's own step 3 applies:
  the current-objective/branch-objective pair, the missing-file fallback, and the open/close scripts. You write
  objective Checks and may run the scripted close yourself, later, in Phase 5.
- import:skill/grimorio.agent-tiers/project.refusal-pattern.md — the shared triad/boundary test both refusals
  above apply.
- **NEVER load `loop-and-graph`, `fan-out`, or `agent-tiers`'s own tier scale here** — Phase 2's own question,
  not this one; pulling them in now is the flat-mega-load anti-pattern this whole rewrite exists to fix.

## PHASE 1 DELIVERABLE — do not read Phase 2 until this is filled

```
BRIEF READ:              <path to tmp/<your-id>/brief.md, confirmed opened in full — or "paragraph only, per
                         the fallback" naming what you were actually handed>
OBJECTIVE FILES READ:    <.claude/current-objective.md + objectives/<branch>.md, both confirmed read — or the
                         named default you are proceeding under per step 3's own missing-file fallback>
WORKTREE CONFIRMED:      <Y — you are running in your own worktree or created one / STOP — say so to your
                         parent, this chain ends here>
REFUSALS APPLIED:        <"None fired — proceeding" / the refusal that fired, verbatim RESPONSE + CALLER-FIX,
                         chain ends here>
OBJECTIVE:               <what your brief actually asks, widened per Core Rule 3 if narrower than the
                         principal's own objective>
EXIT CONDITION:          <every numbered check in your brief holding simultaneously, per Core Rule 4 — full
                         statement lives at Phase 4>
```

## Hard hand-off

**WHEN no refusal fired above ⟶ ALWAYS read
ref:skill/grimorio.flow-delegation/delegate-phases/phase-2-decompose-and-plan.md next, carrying forward: the
brief's content, the objective/exit condition stated above, and confirmation you are in an owned worktree.**
Phase 2 consumes all of it — none of it is re-derived there. **WHEN a refusal fired above ⟶ this chain ends
here; report the refusal per
ref:skill/grimorio.flow-delegation/delegate-phases/phase-5-close-verified-or-could-not.md's own OUTPUT shape
rather than continuing to Phase 2.**
