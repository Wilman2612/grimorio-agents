# Delegate — Phase 2: DECOMPOSE-AND-PLAN

**NEVER read ref:skill/grimorio.flow-delegation/delegate-phases/phase-3-execute.md until THIS phase's own
DELIVERABLE block, below, is actually filled in — not summarized, not promised, filled.**

## The question this phase answers

What are the real, independent pieces of this task, and who — if anyone — does each piece? A genuinely
different question from Phase 1's "may I begin" and Phase 3's "do the work": this phase produces the PLAN (what
the items are, what proves each) and the GRAPH (who gets spawned for which), never either instead of the other.

## Why this phase exists as its OWN, separate, hard-hand-off file — the known error (dimension c)

**MEASURED, across 36 real `grimorio.delegate` spawns, at a real 0/36 (0%): two of the nine knowledge-load
obligations named in this agent's Knowledge block — `agent-selection` and `agent-tiers` — never fired even
once.** Three more — `fan-out`, `report-design`, `working-memory` — are named here as suspected, not
equally-proven: they are NOT measured anywhere in the cited derivation at all. `loop-and-graph` is a separate
case again, grounded by a weaker, single N=1 cue-blind probe (2026-08-15), never the same 36-spawn instrument:
**that probe found a fix already tried WITHIN the flat-STEPS shape — moving `loop-and-graph`'s load into an
explicit numbered step instead of a background Knowledge-block line — was independently re-probed and ALSO
failed: the behavior file itself was never opened that run.** Full derivation, with each item's own actual
evidence class: ref:skill/grimorio.agent-writing/project.carrier-placement.md.
**NEVER read this phase's own hard hand-off as ceremony — it is the direct, measured fix for a failure this
exact agent has already produced twice, under two different mitigations.** This phase's own separate file,
revealed only once Phase 1's own DELIVERABLE exists, makes skipping this load structurally HARDER to fall into
than a bare Core-rule line buried in a flat file ever was — never read as making it impossible: nothing in this
chain executes a mechanical gate on the hand-off itself (the fingerprint-checker mechanism that would is
deliberately not wired this pass, per ref:skill/grimorio.flow-delegation/delegate-phases/delegate-quasi-software-view.md's
own "fingerprint-gate mechanism" section), so an agent that never opens this file at all is still an open
failure mode this rewrite reduces, not one it forecloses.

## LOOP + RELATIONSHIPS threaded here — to your CHILDREN, for real (dimension b)

This is the first phase in the chain where `grimorio.delegate`'s relationship to its CHILDREN actually fires:
step 3 below declares the fan-out graph with a TIER field per node; step 4 dispatches already-planned mechanical
volume at an explicit Haiku override. `grimorio.delegate` is NOT hard-locked non-recursive — it may spawn any
agent type other than its own — so this relationship is accounted for here, and again in Phase 3, never by
assertion.

## Steps

1. **ALWAYS state this phase's own graph before doing anything else: SELF (load `loop-and-graph`, decompose) →
   conditionally a MECH-CHILD per already-planned mechanical item (this phase's own step 4).** A spawn here is a
   choice this phase's own step 4 makes only where a slice is genuinely already-planned mechanical volume —
   never the node this phase opens with.
2. **BEFORE you work the first of your brief's numbered checks ⟶ load import:skill/grimorio.loop-and-graph IN
   FULL and run its machine over your own task.** This fires right after Phase 1's own objective/exit-condition
   statement and before any check is worked.

   **These are two artifacts, not one, and the decomposition comes first.** The plan this step produces names
   WHAT the items are and what proves each; the fan-out graph (step 3 below,
   ref:skill/grimorio.fan-out#emit-the-loop-graph-before-you-spawn-or-write-hard-rule-ceo-2026-08-08) names WHO
   gets spawned, and is emitted for the items this decomposition produced — never instead of it, and never
   before it. **WHEN an item turns out to need no spawn ⟶ it gets a plan entry and no graph entry** — the
   normal case, not an omission.
3. **ALWAYS declare your fan-out graph before you execute.** The general obligation is not yours alone — it
   binds every spawn-capable agent, you included, and it is stated ONCE, not here ->
   ref:skill/grimorio.fan-out#emit-the-loop-graph-before-you-spawn-or-write-hard-rule-ceo-2026-08-08 carries the
   schedule, the five fields, and where the graph lives. Read it there before you act — a copy of its trigger in
   this file is exactly the paraphrase that drifts. What is specific to YOU: when your graph's NODES route to
   down-tiered WORKER children (`model: "haiku"`) for volume, name them as such in the TIER field, with the
   reason. Cases that are volume by definition here: several independent checks or file-level changes within
   your task, running a suite and reporting, a mechanical patch repeated across files. Declare each such child a
   CHILD that must not spawn itself, each on its own path. You are the expensive tier because you do the
   deciding — the saving comes from the volume going DOWN-tier, never from under-tiering yourself.
4. **WHEN a piece of your task is already-PLANNED, mechanical volume ⟶ dispatch ONE child per already-decided
   piece at an explicit `model: "haiku"` override, no named reason needed — review it after, retry bound
   TWO-TO-THREE attempts, then finish it yourself.** Apply
   ref:skill/grimorio.fan-out#not-every-task-is-a-fan-out--the-single-child-shape-ceo-2026-07-30's shape, NOT the
   volume-fan-out ladder — that ladder is scoped to an agent spawning children of its OWN type, and you are
   forbidden from ever doing that. **Reserve an UPWARD model override for a task you can NAME a reason for** —
   ref:skill/grimorio.agent-tiers#the-orchestration-cascade-cost-discipline--enforce-it measures it the rarest
   override in the system.

## Core Rule 2 threaded here — foreground default, its graph/TIER half

Core Rule 2's full statement lives at ref:skill/grimorio.flow-delegation/delegate-phases/phase-3-execute.md —
this phase carries only its graph-declaration half: **foreground stays your safe default; backgrounding your
own children for real parallelism is a considered trade, declared as such in the TIER field above, never a
silent default.**

## LOAD (JIT) — scoped to this phase only

- import:skill/grimorio.loop-and-graph — the machine you run to own a task end to end; per step 2 above, loaded
  and run IN FULL here, never deferred and never skipped.
- import:skill/grimorio.fan-out — binds you on BOTH halves. Part 1
  (ref:skill/grimorio.fan-out#part-1--decompose-spawn-in-parallel-synthesize) is the parallelism imperative this
  phase's own graph declaration applies. Part 2
  (ref:skill/grimorio.fan-out#part-2--stay-reachable-report-back-without-parking) is your operating plumbing —
  your id, your workspace (`tmp/<your-id>/`), the notes-folder protocol — carried into Phase 3, where it is
  actually used.
- import:skill/grimorio.agent-tiers — the lever that lets you scale execution down at low cost; step 4 above is
  where it is applied.
- ref:skill/grimorio.agent-selection — WHICH agent to raise, and WHEN, for any node in your graph that is NOT
  already-planned mechanical volume. Match an agent's CONTRACT, never its name or area; use the ESCALATION
  LADDER (agent-selection → "The ESCALATION LADDER") when you are stuck. NEVER `general-purpose` as a grunt.
- **NEVER load `code-harness`, `working-memory`'s own scratch convention, or `report-design` here** — each is a
  later phase's own question.

## PHASE 2 DELIVERABLE — do not read Phase 3 until this is filled

```
LOOP-AND-GRAPH LOADED:   <Y — confirm the full skill was actually opened this pass, never assumed carried>
DECOMPOSITION:           <the real items this task splits into — WHAT each is and what proves it, per
                         loop-and-graph's own machine>
FAN-OUT GRAPH:           <one line per node: item -> agent type -> TIER (with reason if down-tiered) -> spawn
                         timing (Phase 2 now, or Phase 3 later) — "No spawn needed this pass" is a legitimate
                         entry for an item Phase 3 will simply execute directly>
MECHANICAL-VOLUME DISPATCHED: <per step 4 — child(ren) dispatched at model:"haiku", retry count used (0-3), or
                         "N/A — no already-planned mechanical volume this pass">
```

## Hard hand-off

**ALWAYS read ref:skill/grimorio.flow-delegation/delegate-phases/phase-3-execute.md next, carrying forward: the
decomposition, the fan-out graph, and any mechanical-volume child(ren) already dispatched above.** Phase 3
executes against this plan — it does not re-derive it.
