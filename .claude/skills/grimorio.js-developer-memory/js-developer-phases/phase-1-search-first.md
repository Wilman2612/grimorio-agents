# Backend TS Developer — Phase 1: SEARCH-FIRST

**NEVER read
ref:skill/grimorio.js-developer-memory/js-developer-phases/phase-2-plan-read-arch-decision.md until THIS phase's
own DELIVERABLE block, below, is actually filled in — not summarized, not promised, filled.** Phase 2 consumes
this phase's own trap/precedent findings while reading the architecture contract; reading ahead without them is
scoping blind to gotchas this project has already paid for once.

## The question this phase answers

What does this project's own trap history already know about the specific area this task touches, before the
architecture contract is even read? This phase does not read `arch-decision.md`, does not determine MODE, and
does not scope a single file or module — it only searches this agent's own two-file trap corpus and the shared
cross-language trap file for precedent, so Phase 2's contract-read is never done blind to a gotcha this project
has already hit once.

## Steps

1. **ALWAYS state this phase's own graph before doing anything else: a single SELF node — select which trap
   file(s) this task's own area touches, search them, search the shared cross-language traps, note precedent —
   and nothing else; no spawn anywhere in this phase.** This agent never invokes another agent from Phase 1 —
   the only agent this chain ever spawns is a same-type `haiku` child from Phase 3's own FAN-OUT BRANCH, never
   from here.
2. **ALWAYS select which trap file(s) this task's own invocation touches, and state the selection explicitly as
   its own step — this is a genuine judgment call THIS agent's own two-file split requires that
   `grimorio.go-developer`'s own single trap log never needed.** The area/file split, per
   this project's own developer memory: a task touching the
   shared TypeScript packages or `apps/web`'s own server-side layers ⟶ search this agent's own primary trap log; a task touching
   `services/runner-node` specifically ⟶ search this agent's own runner-node trap log; a task touching BOTH areas ⟶ search
   both. **NEVER guess the split from the task's own title alone — verify against the concrete folder paths in
   the pointer above before selecting.**
3. **ALWAYS search the selected trap file(s) from step 2, targeted at this task's own specific area — never a
   full unfocused read of a 540-line/48KB or 73-line/6KB file end to end when the task names a narrow slice.**
   Note every finding relevant to this task's own scope, or state plainly that none were found.
4. **ALWAYS ALSO search this project's own developer trap log** (the shared cross-language trap
   file every developer agent owes) **for a precedent relevant to this task's own area — a money/DB/LLM-frontier
   gotcha, a determinism gotcha, a cross-service wire-format trap.** Note every finding, or state plainly that
   none were found.
5. **ALWAYS surface whatever ADJACENT precedent already exists beyond the trap files themselves — a comparable
   prior change to this same area, a comparable contract this project has already wired once** — so Phase 2's
   contract-read is not starting cold on a pattern this project has already established.

## LOAD (JIT) — scoped to this phase only

- this agent's own two trap logs (primary and runner-node) — whichever step 2 above selects (one or both) —
  this agent's own concrete TS/JS traps, split by project area.
  FINGERPRINT: TRAPS CHECKED (OWN) field below (a real, targeted search result naming which file(s) were
  searched and what was found, distinct from an unchecked "none," cannot be produced without opening the
  selected file(s)).
- this project's own developer trap log — the shared cross-language trap file, step 4's own
  load.
  FINGERPRINT: TRAPS CHECKED (SHARED) field below (a real search result, distinct from an unchecked "none,"
  cannot be produced without opening this file).
- **NEVER load `arch-decision.md`, the wire/DAL contracts, `javascript`, `development-patterns`, the fan-out
  ladder, or the Definition-of-Done checklist here** — none of those are this phase's question, and pulling any
  of them in now is exactly the flat-mega-load anti-pattern ref:skill/grimorio.phase-splitting exists to fix.

## PHASE 1 DELIVERABLE — do not read Phase 2 until this is filled

```
OBJECTIVE:                <what this invocation was actually asked to do, taken from the brief>
EXIT CONDITION:            <the checkable state that means the objective holds — the completion criteria Phase 5
                           below states in full>
TRAP FILE(S) SELECTED:      <this agent's own primary trap log and/or its own runner-node trap log, per step 2, with the one-line reasoning
                           for the selection>
TRAPS CHECKED (OWN):         <what was searched in the selected file(s) above, and what was found — or "None
                           found relevant to this task's own area", per step 3>
TRAPS CHECKED (SHARED):        <what was searched in this project's own developer trap log, and what was found — or
                           "None found relevant", per step 4>
PRECEDENT NOTED:                 <adjacent prior precedent for this task's own area, per step 5, or "None found">
```

## Hard hand-off

**BEFORE reading ref:skill/grimorio.js-developer-memory/js-developer-phases/phase-2-plan-read-arch-decision.md
⟶ apply import:skill/grimorio.phase-splitting/project.fingerprint-gate.md against THIS file
(`ref:repo/.claude/skills/grimorio.js-developer-memory/js-developer-phases/phase-1-search-first.md`) and this
phase's own filled PHASE 1 DELIVERABLE block, written to disk first per that gate's own algorithm — the read
below now runs on that gate's own PASS, never on the block merely existing in context.**

**ALWAYS read
ref:skill/grimorio.js-developer-memory/js-developer-phases/phase-2-plan-read-arch-decision.md next, carrying
forward: OBJECTIVE, EXIT CONDITION, TRAP FILE(S) SELECTED, TRAPS CHECKED (OWN), TRAPS CHECKED (SHARED), and
PRECEDENT NOTED, unconditionally.** Phase 2 consumes all of it while reading the architecture contract — none of
it is re-derived there.
