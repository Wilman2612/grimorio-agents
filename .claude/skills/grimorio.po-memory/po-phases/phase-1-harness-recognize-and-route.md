# Product Owner — Phase 1: HARNESS — RECOGNIZE & ROUTE (H1)

**NEVER read ref:skill/grimorio.po-memory/po-phases/phase-2-harness-verify-and-capture.md until THIS phase's own
DELIVERABLE block, below, is actually filled in — not summarized, not promised, filled.** Nothing mechanically
gates this; the gate is that you do not open the next file until you have produced what this one asks for.

## The question this phase answers

What was actually just decided or expressed — a settled product decision vs a vision/roadmap/ambition
statement vs the Grimorio meta-system's own vision — and which of the three memory files does it belong to?
Nothing about writing the entry, running the custody check, or resolving Currency belongs here — only
recognition and routing.

## Standing boundary, restated

**NEVER decide anything about PO's own charter, tier, or scope.** That is the CEO's call alone, unaffected by
anything recognized or routed here.

## Standing responsibility — keep the account of the user's VISION

Beyond capturing settled product decisions, you hold the durable record of the user's **vision, roadmap, and
ambition**. Whenever the user expresses vision, roadmap, future direction, not-yet-planned-but-will-do
projects, the shape of the product to come, or ambition — **even implicitly, and even when it is NOT a
concrete feature request** — RECORD it in this project's own product-vision record (its own "Classification"
section, by axis and where each section's text lives) (append/refine; mark speculative items **[maybe]**). This is the **CEO's** vision, not yours to decide — you
are the scribe that keeps it so it **survives context resets** and the user never has to re-explain it (a
recurring, real cost). This is a standing duty, independent of, and more frequent than, writing a
`po-brief.md`. When in doubt whether something is "vision," record it — an extra line is cheap; a forgotten
vision that gets re-explained for the tenth time is not.

Two vision records exist, for two different things — this phase's own routing step below decides which: the
**product** vision / roadmap / ambition lives in this project's own product-vision record (its own
"Classification" section, by axis and where each section's text lives). The **Grimorio meta-system** vision
(the agent/skill system used to build the product — how agents are selected, the research flow, planned
agents — NOT product) lives in this project's own Grimorio meta-system vision record (**provisional**,
PO-kept until it graduates to its own home, likely under
ref:skill/grimorio.agent-writing#the-split-principle--the-agent-is-who-it-is-the-skill-is-what-it-does).

## Steps

1. **ALWAYS state this phase's own graph before doing anything else: a single SELF node — lightly recognize
   what kind of statement this is, grep the now-identified candidate memory file, then formally route it — and
   nothing else; no spawn, ever, in this phase (the optional scout lives at H3, never here).** Recognizing WHICH
   of the three candidate memory files is even relevant is a lightweight, informal read of the user's own
   words — it is NOT yet Step 5's formal routing decision, only enough to know what to grep — so it runs FIRST;
   only once a candidate target is named does this phase's own required SEARCH-FIRST opening fire: **BEFORE
   routing or writing anything ⟶ grep that now-identified candidate memory file**, per grimorio-conduct rule 22
   ("before adding to any memory or vision file, grep it for what is there and say what you found") — state
   what was found. Grepping a target before any candidate has been named would grep nothing real; naming a
   candidate before grepping it would violate rule 22's grep-before-add discipline — this order is the only one
   that satisfies both.
2. **BEFORE anything else ⟶ state OBJECTIVE (the settled decision or vision statement being captured, taken
   verbatim from the request) and EXIT CONDITION (what the routing decision must identify for it to hold)**,
   per Core rule 1 in Phase 0.
3. **ALWAYS recognize a product decision, priority call, or vision/roadmap/ambition statement in the user's own
   words — even implicit, even not-yet-a-feature — as something owed a capture**, per the Standing
   responsibility above. This lightweight recognition is enough to name WHICH candidate memory file (`project.md`
   / `project.vision.md` / `project.grimorio-vision.md`) the statement belongs to — it is not yet Step 5's
   formal routing decision.
4. **BEFORE routing or writing anything ⟶ grep the candidate memory file step 3 just identified**, per
   grimorio-conduct rule 22 — state what was found. This is this phase's own required SEARCH-FIRST opening,
   satisfied only now that a candidate target actually exists to grep.
5. **ALWAYS route what step 3 recognized, confirmed against what step 4's own grep found: a settled product
   decision ⟶ this project's own product memory; product vision/roadmap/ambition ⟶ this project's own
   product-vision record (its own "Classification" section); the Grimorio meta-system's own vision (NOT
   product) ⟶ this project's own Grimorio meta-system vision record** (provisional, PO-kept until it
   graduates to its own home).

## LOAD (JIT) — scoped to this phase only

- this project's own product-vision record (its own "Classification" section) — the classification axis,
  needed nowhere else in this chain.
- **NEVER load the custody-check rule, the Currency rule, or the Maintenance rule here** — those are H2's and
  H3's own JIT slices; loading them here would defeat the whole point of just-in-time loading.

## PHASE 1 DELIVERABLE — do not read Phase 2 until this is filled

```
OBJECTIVE:              <the settled decision or vision statement being captured, verbatim from the request>
EXIT CONDITION:          <what the routing decision must identify for it to hold>
RECOGNIZED STATEMENT:    <the decision/vision/meta-vision content, named plainly — step 3's lightweight read,
                         naming the CANDIDATE memory file this identifies before anything is grepped>
GREP RESULT:             <what step 4's SEARCH-FIRST grep of that now-identified candidate file found, before
                         anything is routed or written>
ROUTING TARGET:          <the exact file path from step 5, confirmed against the grep above>
```

## Hard hand-off

**ALWAYS read ref:skill/grimorio.po-memory/po-phases/phase-2-harness-verify-and-capture.md next, carrying
forward the recognized statement and the routing target above.** Phase 2 acts on exactly what this phase
decided — it does not re-recognize or re-route.
