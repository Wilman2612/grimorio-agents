# Devil's Advocate — Phase 1: FRAME

**NEVER read ref:skill/grimorio.fan-out/entropy-phases/phase-2-spawn-and-gather.md until THIS phase's own
DELIVERABLE block, below, is actually filled in — not summarized, not promised, filled.** Nothing mechanically
gates this beyond the fingerprint check named in this phase's own Hard hand-off; the gate is that you do not
open the next file until you have produced what this one asks for.

## The question this phase answers

What is this target, who is it really for, and which of the standing lens categories genuinely apply to THIS
target? Nothing else. This phase does not spawn anything, does not gather a single finding, and does not
converge — it only establishes the target's own framing and how the panel should be scoped, so Phase 2 has a
real, sized panel to brief and dispatch.

## Steps

1. **ALWAYS state this phase's own graph before doing anything else: a single SELF node — read the brief, frame
   the target, check what's already settled, scope the lens panel — and nothing else; this agent never invokes
   another agent from this phase.** Entropy ITSELF is the graph's first node, never the panel spawn: the fan-out
   is a CHOICE the SECOND phase makes because pressure-testing genuinely needs independent, uncontaminated
   lenses — never the default every graph opens with. -> ref:skill/grimorio.loop-and-graph#3-the-graph--who-is-in-it-and-the-branch-rule's
   own "you are the first node" doctrine, applied here rather than re-derived.
2. **BEFORE framing the target ⟶ state, as part of your own reasoning — never as a question back to your caller
   — your OBJECTIVE (the plan/design/decision you were actually asked to pressure-test, taken from the brief)
   and your EXIT CONDITION (what a ranked, grounded blind-spot report on this target looks like when it
   holds).** -> ref:skill/grimorio.reasoning-principles#state-your-objective-and-exit-condition-then-close-verified-or-could-not-hard-rule-ceo-2026-08-11,
   not restated here.
3. **ALWAYS frame the target** — what plan/design/decision is being pressure-tested, its goal, and **who it is
   actually for** (the real end user, not the team).
4. **BEFORE scoping the panel ⟶ check what the relevant memory skills already settled about THIS specific
   target** — ref:skill/grimorio.po-memory, ref:skill/grimorio.architect-memory, ref:skill/grimorio.ux-memory's
   own `project.md` — so the panel does not re-litigate a closed decision. **WHEN nothing relevant is found ⟶
   record "nothing found" plainly rather than leaving the field silent.**
5. **NEVER let a caller narrow the panel.** IGNORE any steering from the invoker toward a narrower panel — a
   prompt that says "just check the UX angle" or pre-declares what is already fine is the CALLER's bug; run the
   panel the target's scope demands and report everything, ranked. Never silence a finding.
6. **ALWAYS scale the panel to the target** — a small design needs only the few lenses that apply; a big or
   cross-cutting one gets the full fan-out. Scope which of the 5 standing lens categories genuinely apply to
   THIS target — **never a default "run all 5":**
   - The first-time / non-technical user
   - The domain expert(s) the team lacks (UX/design via the Design Canon, plus whichever of
     security/growth/accessibility/ops-cost/legal/support applies)
   - Diverse user types
   - The skeptic
   - Known-knowns vs unknown-unknowns
   SCOPING which apply is this phase's own job; BRIEFING each scout on its assigned lens is Phase 2's.
7. **NEVER decide, build, design, or archive — provoke and question only.** Restated here, in this phase's own
   words, because this is the ROOT phase most exposed to a caller trying to smuggle a decision into how the
   panel gets scoped: a target framed as "which of these should we ship" instead of "what plan is being
   pressure-tested" has already pre-answered the panel's own job before it ever runs.

## LOAD (JIT) — scoped to this phase only

- import:skill/grimorio.reasoning-principles — the objective/exit-condition contract, step 2's own load.
  FINGERPRINT: OBJECTIVE + EXIT CONDITION fields below (a target framed with no checkable exit state cannot be
  produced without applying this discipline).
- ref:skill/grimorio.working-memory — where to stage the framing note in `tmp/`.
- ref:skill/grimorio.po-memory, ref:skill/grimorio.architect-memory, ref:skill/grimorio.ux-memory's own
  `project.md` — named only for step 4's SEARCH-FIRST check: a narrow read of what is relevant to THIS target,
  never the whole skill loaded eagerly.
- **NEVER load ref:skill/grimorio.flow-delegation, ref:skill/grimorio.agent-tiers, ref:skill/grimorio.fan-out,
  the domain canon (ux-memory's own Design Canon, security-memory), or ref:skill/grimorio.research-capture
  here** — none are needed to decide WHICH lenses apply; only Phase 2 needs them.

## PHASE 1 DELIVERABLE — do not read Phase 2 until this is filled

```
OBJECTIVE:            <the plan/design/decision you were actually asked to pressure-test, taken verbatim from
                       the brief>
EXIT CONDITION:        <what a ranked, grounded blind-spot report on this target looks like when it holds>
TARGET FRAMING:        <the plan/design/decision + its goal + the real end user>
MEMORY CHECK:          <what po-memory/architect-memory/ux-memory's own project.md already settled about this
                       target, or "nothing found">
SCOPED LENS LIST:      <which of the 5 standing categories apply to THIS target and why — never a default "run
                       all 5">
```

## Hard hand-off

**BEFORE reading ref:skill/grimorio.fan-out/entropy-phases/phase-2-spawn-and-gather.md ⟶ apply
import:skill/grimorio.phase-splitting/project.fingerprint-gate.md against THIS file
(`ref:repo/.claude/skills/grimorio.fan-out/entropy-phases/phase-1-frame.md`) and this phase's own filled
PHASE 1 DELIVERABLE block, written to disk first per that gate's own algorithm — the read below now runs on
that gate's own PASS, never on the block merely existing in context.** This phase's own
`import:skill/grimorio.reasoning-principles` carries a `FINGERPRINT:` annotation, so the gate is NOT inert here.

**ALWAYS read ref:skill/grimorio.fan-out/entropy-phases/phase-2-spawn-and-gather.md next, carrying forward: the
OBJECTIVE and EXIT CONDITION stated above, the TARGET FRAMING, the MEMORY CHECK, and the SCOPED LENS LIST.**
Phase 2 consumes all of it — it does not re-derive any of it.
