# Experimenter — Phase 1: SEARCH-FIRST

**NEVER read ref:skill/grimorio.experiment-method/experimenter-phases/phase-2-pre-registration.md until THIS
phase's own DELIVERABLE block, below, is actually filled in — not summarized, not promised, filled.** Nothing
mechanically gates the READ itself; the gate that DOES fire mechanically is the fingerprint check on the PROCEED
edge below.

## The question this phase answers

Has this hypothesis already been settled, does grimorio already know anything relevant, and is this invocation
even the kind that earns a paper at all? Nothing else. This phase does not pre-register a hypothesis, does not
decide a regime, and does not run anything — it only establishes the facts Phase 2 needs before it plans
against them, and it is the ONE place a non-hypothesis invocation gets caught before any of that planning
happens.

## Steps

1. **ALWAYS state this phase's own graph before doing anything else: a single SELF node — read the brief, read
   the method, read the index/prior papers — and nothing else; this agent never invokes another agent, in any
   phase, ever** (no `Agent` tool, confirmed in your own shell's `tools:` line).
2. **BEFORE anything else ⟶ apply the auto-invocation precision boundary, verbatim:** "you produce a paper only
   when a real design HYPOTHESIS is being settled by a run. A QA smoke run, a one-off sanity simulation run, or
   a build check does NOT trigger a paper." **WHEN this invocation does not meet that boundary ⟶ this is a
   terminal EARLY-EXIT — no paper, no phase chain beyond this one.** State that disposition explicitly in this
   phase's own DELIVERABLE below and do NOT proceed to Phase 2.
3. **WHEN the boundary IS met ⟶ state your OBJECTIVE** (the design question the brief asked you to settle,
   verbatim) **and your EXIT CONDITION** (paper + companion written, INDEX updated, the hypothesis's fate stated
   with its deciding evidence — OR, if Phase 2 finds the harness cannot meet the method, the confound stated
   explicitly), per
   ref:skill/grimorio.reasoning-principles#state-your-objective-and-exit-condition-then-close-verified-or-could-not-hard-rule-ceo-2026-08-11.
4. **ALWAYS read this project's own experiment lab record in full** — this project's lab, paths, index,
   acceptance bars, and invocation trigger — read live via Read, never `Skill()`.
5. **ALWAYS read this project's own experiment catalogue and check for an already-recorded paper on the
   SAME hypothesis.** "Never re-run an experiment already recorded (check the index first); extend its paper
   instead. Never re-derive a settled decision" — verbatim carry-forward from the prior process.
6. **ALWAYS read any signed design doc under test the brief points at** (a `tmp/` doc or a
   ref:skill/grimorio.po-memory reference).
7. **ALWAYS confirm, explicitly, that this agent never spawns** — the CHILDREN relationship is trivially
   satisfied, restated here as this phase's own step rather than assumed carried from Phase 0 alone.

## LOAD (JIT) — scoped to this phase only

- import:skill/grimorio.reasoning-principles — objective/exit-condition contract only,
  ref:skill/grimorio.reasoning-principles#state-your-objective-and-exit-condition-then-close-verified-or-could-not-hard-rule-ceo-2026-08-11.
  FINGERPRINT: OBJECTIVE + EXIT CONDITION fields below (a real objective/exit-condition pair, on the PROCEED
  path, cannot be produced without applying this discipline).
- this project's own experiment lab record — read in full, step 4.
- this project's own experiment catalogue — step 5.
- import:skill/grimorio.working-memory — the `tmp/` working-folder convention for the whole chain, loaded ONCE
  here.
- **NEVER load the statistics/metrics apparatus, the paper-structure contract, or the regime-decision doctrine
  here** — none of it is this phase's question.

## PHASE 1 DELIVERABLE — do not read Phase 2 until this is filled

```
OBJECTIVE:                 <the design question the brief asked you to settle, verbatim — "N/A, EARLY-EXIT" on
                           the early-exit path>
EXIT CONDITION:             <paper + companion + INDEX + hypothesis's fate, per step 3 — "N/A, EARLY-EXIT" on
                           the early-exit path>
PRECISION-BOUNDARY CHECK:   <met / NOT met — terminal EARLY-EXIT, per step 2>
NOVELTY CHECK:              <already recorded in INDEX.md under slug X — extend that paper / genuinely novel>
PROJECT LAB FACTS CARRIED:  <acceptance bars, lab-limits summary, invocation trigger, from project.md>
CHILDREN CONFIRMATION:      <this agent never spawns — confirmed>
TERMINAL DISPOSITION:       <PROCEED to Phase 2 / EARLY-EXIT — precision boundary not met, no paper produced>
```

## Hard hand-off

**WHEN the TERMINAL DISPOSITION above is EARLY-EXIT ⟶ this chain ends here — report the disposition to your
caller directly, no Phase 2 through Phase 5.**

**WHEN the TERMINAL DISPOSITION above is PROCEED ⟶ the gate below applies before Phase 2 is read.**

**BEFORE reading ref:skill/grimorio.experiment-method/experimenter-phases/phase-2-pre-registration.md ⟶ apply
import:skill/grimorio.phase-splitting/project.fingerprint-gate.md against THIS file
(`ref:repo/.claude/skills/grimorio.experiment-method/experimenter-phases/phase-1-search-first.md`) and this
phase's own filled PHASE 1 DELIVERABLE block, written to disk first per that gate's own algorithm — the read
below now runs on that gate's own PASS, never on the block merely existing in context.** This phase's own
`import:skill/grimorio.reasoning-principles` carries a `FINGERPRINT:` annotation, so the gate is NOT inert here.

**ALWAYS then read ref:skill/grimorio.experiment-method/experimenter-phases/phase-2-pre-registration.md next,
carrying forward: the OBJECTIVE and EXIT CONDITION stated above, the novelty finding, and the project lab
facts.** Phase 2 consumes all of it to plan against — none of it is re-derived there.
